server.port=8080
spring.application.name=ms-random-number

# Exponer únicamente los endpoints no sensibles de Actuator
management.endpoints.web.exposure.include=health,info
# Deshabilitar explícitamente todos los demás endpoints para reducir la superficie de ataque
management.endpoints.web.exposure.exclude=*
management.endpoint.health.show-details=never
